# Scripts

Utility scripts for Jig development.

| Script | Purpose |
|--------|---------|
| `auto_test.py` | Self-test script — runs autopep8 + pytest + examples |
| `AgentHarness.spec` | PyInstaller build config (legacy name, kept for reference) |
